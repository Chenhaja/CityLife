CityHub——智能生活服务平台

仓库地址：https://github.com/Xiaoac7r/CityHub
技术栈： SpringBoot、MySQL、Redis、Lua、MyBatis-Plus、RocketMQ

项目简介：
CityHub是一个本地生活服务平台，提供商家信息查询、优惠券秒杀、推广优惠信息等功能，针对秒杀场景下的性能瓶颈，基于缓存架构与消息队列进行了性能深度优化，显著提升高并发场景下的系统稳定性。

- 设计Redis + Lua实现高并发库存扣减与一人一单校验，解决超卖问题，保障数据一致性。

- 引入RocketMQ将秒杀链路中的下单流程异步解耦，实现核心接口流量削峰并提升秒杀场景并发性能。

- 使用逻辑过期方案防止Redis热点Key的缓存击穿问题，使用缓存空值方案解决Redis Key的缓存穿透问题。

- 更新数据库后删除缓存，删除失败采取RocketMQ补偿重试，结合TTL兜底共同确保数据最终一致性。

- 基于Redis + AOP + 注解实现滑动窗口限流，支持全局/IP/用户多维度，防止系统过载、刷券、爬虫。

- 基于延迟队列实现分钟级定时任务，精准扫描并自动关闭超时未支付订单，有效释放被占用的库存资源。

- 使用乐观锁解决支付回调与超时关单状态下的并发问题，确保订单状态流转的准确性与数据一致性。

专业技能

- 熟练掌握JavaSE核心知识，理解常用集合及数据结构、面向对象、异常、反射、泛型等；

- 熟悉MySQL，理解事务、隔离级别、索引、MVCC、三大日志、锁机制、慢SQL优化等；

- 熟悉Redis，理解其5种核心数据结构、持久化策略、分布式锁、缓存穿透、击穿、雪崩及解决方案等；

- 熟悉RocketMQ消息中间件，理解可靠消息、幂等、事务消息、消息堆积问题及解决方案等；

- 熟悉JUC并发包，了解线程池、CAS、AQS、Lock、ThreadLocal、JMM等；

- 熟悉JVM内存结构，了解类加载机制、双亲委派、垃圾回收、内存模型、调优方向等；

- 熟悉SpringBoot、MyBatis，熟悉事务管理、AOP、IOC、Bean生命周期等；

- 熟悉IDEA、Maven、Git等配套开发工具，熟悉Code、Claude Code等AI编程工具

需要我把这份简历整理成Markdown纯净版，或者精简成适合投递的一页版简历文本吗？



# 设计Redis + Lua实现高并发库存扣减与一人一单校验，解决超卖问题，保障数据一致性。  这一块在代码那里体现

I'll explore the CityHub project to find where inventory deduction and one-order-per-user logic is implemented.
已定位到完整实现链路。这块逻辑主要由 **1 个 Lua 脚本 + 3 个 Java 类** 组成。

## 一、核心位置

### 1. Lua 脚本本体（原子性来源）
`src/main/resources/seckill.lua`

```16:37:src/main/resources/seckill.lua
-- 3.脚本业务
--3.1.判断库存是否充足
local stock = tonumber(redis.call('get', stockKey))
if stock == nil then
    --print("库存获取失败: " .. stockKey)
    return -1
end

if (stock<= 0) then
    --3.2.库存不足，返回1
    return 1
end
--3.3.判断用户是否下单
if(redis.call('sismember',orderKey,userId)==1) then
    --3.4.存在，说明重复下单，返回2
    return 2
end
-- 3.5.扣库存 incrby stockKey -1
redis.call('incrby',stockKey,-1)
-- 3.6.下单(保存)用户 sadd orderKey userId
redis.call('sadd',orderKey,userId)
-- 3.7. 发消息到消息队列中 XADD  stream.orders * k1 v1 k2 v2
redis.call('xadd','stream.orders','*', 'userId',userId,'voucherId',voucherId,'id',orderId)
```

关键点：**「查库存 → 判重 → 扣库存 → 写订单 Set」四步在一个 Lua 脚本里，Redis 单线程执行，天然原子**，不存在 check-then-act 的竞态，这是解决超卖的根本。

### 2. 脚本加载与调用
`src/main/java/com/hmdp/service/impl/VoucherOrderServiceImpl.java`

```67:72:src/main/java/com/hmdp/service/impl/VoucherOrderServiceImpl.java
    private static final DefaultRedisScript<Long> SECKILL_SCRIPT;
    static {
        SECKILL_SCRIPT=new DefaultRedisScript<>();
        SECKILL_SCRIPT.setLocation(new ClassPathResource("seckill.lua"));
        SECKILL_SCRIPT.setResultType(Long.class);
    }
```

```206:220:src/main/java/com/hmdp/service/impl/VoucherOrderServiceImpl.java
        Long result = stringRedisTemplate.execute(
                SECKILL_SCRIPT,
                Collections.emptyList(),
                voucherId.toString(), userId.toString(),String.valueOf(orderId)
        );
        //2.判断结果是否为0
        int r = 0;
        if (result != null) {
            r = result.intValue();
        }
        if(r!=0){
            //2.1.不为0，代表没有购买资格
            return Result.fail(r==1?"库存不足":"不能重复下单");
        }
```

这里 `static` 块预加载脚本，避免每次传输脚本体；返回码语义：`0` 成功 / `1` 库存不足 / `2` 重复下单 / `-1` 库存 key 不存在。

### 3. 库存预热（Redis 库存的来源）
`src/main/java/com/hmdp/service/impl/VoucherServiceImpl.java`

```55:57:src/main/java/com/hmdp/service/impl/VoucherServiceImpl.java
        seckillVoucherService.save(seckillVoucher);
        //保存秒杀得库存到redis
        stringRedisTemplate.opsForValue().set(SECKILL_STOCK_KEY +voucher.getId(),voucher.getStock().toString());
```

Key 常量在 `RedisConstants.SECKILL_STOCK_KEY = "seckill:stock:"`。

### 4. 异步落库 + 数据库层兜底
`src/main/java/com/hmdp/listener/SeckillVoucherListener.java`

```40:47:src/main/java/com/hmdp/listener/SeckillVoucherListener.java
        voucherOrderService.save(voucherOrder);
        // 数据库秒杀库存减一
        Long voucherId = voucherOrder.getVoucherId();
        seckillVoucherService.update()
                .setSql("stock = stock - 1")
                .eq("voucher_id", voucherId)
                .gt("stock", 0)
                .update();
```

`.gt("stock", 0)` 是 DB 侧的**乐观锁/条件更新**，防止库存被扣成负数。

另外 `VoucherOrderServiceImpl` 里还保留了同步兜底路径：`handleVoucherOrder()`（175-198，Redisson 分布式锁 `lock:order:{userId}`）+ `createVoucherOrder()`（288-314，`@Transactional` + 先查重再条件扣库存）。

---

## 二、数据一致性保障（超时关单回补）

```334:352:src/main/java/com/hmdp/service/impl/VoucherOrderServiceImpl.java
    @Transactional
    public boolean closeTimeoutOrder(Long orderId) {
        VoucherOrder order = getById(orderId);
        if (order == null || order.getStatus() == null || order.getStatus() != 1) {
            return false;
        }
        boolean updated = update()
                .eq("id", orderId)
                .eq("status", 1)
                .set("status", 4)
                .update();
        if (updated) {
            seckillVoucherService.update()
                    .setSql("stock = stock + 1")
                    .eq("voucher_id", order.getVoucherId())
                    .update();
        }
        return updated;
    }
```

配合 `OrderTimeoutListener`（RocketMQ 延迟消息）与 `payCallback` 的状态机乐观锁（`eq("status", 1)`）实现幂等流转。

---

## 三、发现的几个问题（建议修复）

| #    | 问题                                                         | 位置                              |
| ---- | ------------------------------------------------------------ | --------------------------------- |
| 1    | **DB 层缺唯一索引**：`tb_voucher_order` 只有主键，没有 `(user_id, voucher_id)` 唯一约束。Redis 挂了/绕过 Lua 直接写库时，一人一单会失效 | `db/hmdp.sql:1265-1278`           |
| 2    | **`-1` 返回码被误判**：库存 key 不存在返回 `-1`，但 Java 侧 `r != 0` 时统一提示 `"不能重复下单"`，错误信息不准确 | `VoucherOrderServiceImpl:217-220` |
| 3    | **超时关单只回补 MySQL，没回补 Redis**，且没有 `srem` 移除用户下单记录 → 库存回补后该用户无法再次抢购 | `VoucherOrderServiceImpl:345-349` |
| 4    | **Lua 里 `xadd stream.orders` 是残留代码**，消费端已改为 RocketMQ，Stream 消息无人消费，白白占用内存 | `seckill.lua:37`                  |
| 5    | **`SeckillVoucherListener` 无幂等**：MQ 重复投递会重复扣库存 | `SeckillVoucherListener:36-47`    |

需要我把这几项修复吗？



# 引入RocketMQ将秒杀链路中的下单流程异步解耦，实现核心接口流量削峰并提升秒杀场景并发性能。 这一块代码在哪里

RocketMQ 异步解耦这块代码分布在 **6 个位置**，形成一条「生产者 → 消费者 → 延迟消息消费者」的完整链路。

## 一、依赖与配置

**依赖** `pom.xml`

```87:91:pom.xml
        <dependency>
            <groupId>org.apache.rocketmq</groupId>
            <artifactId>rocketmq-spring-boot-starter</artifactId>
            <version>2.2.3</version>
        </dependency>
```

**连接配置** `src/main/resources/application.yaml`

```23:26:src/main/resources/application.yaml
  rocketmq:
    name-server: 127.0.0.1:9876
    producer:
      group: seckill-order-producer
```

---

## 二、生产者：同步转异步的「削峰点」

`src/main/java/com/hmdp/service/impl/VoucherOrderServiceImpl.java`

```225:239:src/main/java/com/hmdp/service/impl/VoucherOrderServiceImpl.java
        // 2. 脱离请求线程，发消息给 RocketMQ
        VoucherOrder order = new VoucherOrder();
        order.setId(orderId);
        order.setUserId(userId);
        order.setVoucherId(voucherId);
        // 放入 RocketMQ 异步下单
        String jsonStr = JSONUtil.toJsonStr(order);
        try {
            rocketMQTemplate.convertAndSend("seckill-order-topic", jsonStr);
        } catch (Exception e) {
            log.error("发送 RocketMQ 消息失败，订单ID: {}", orderId, e);
            throw new RuntimeException("发送消息失败");
        }
        // 3. 返回订单号给前端（实际下单异步处理）
        return Result.ok(orderId);
```

**这就是削峰的核心**：请求线程只做「Lua 资格校验 + 发一条 MQ 消息」，随后立即返回。DB 的 `insert 订单` + `update 库存` 全部下沉到消费者，按消费能力匀速处理，MySQL 不会被瞬时流量打穿。

被替换掉的同步版本仍以注释形式保留在同文件 `243-286` 行（`@Transactional` 直接落库 + Redisson 锁），可作为改造前后的对比。

---

## 三、消费者：异步落单

`src/main/java/com/hmdp/listener/SeckillVoucherListener.java`

```25:47:src/main/java/com/hmdp/listener/SeckillVoucherListener.java
@RocketMQMessageListener(topic = "seckill-order-topic", consumerGroup = "seckill-order-consumer")
public class SeckillVoucherListener implements RocketMQListener<String> {
    ...
    public void onMessage(String msg) {
        VoucherOrder voucherOrder = JSONUtil.toBean(msg, VoucherOrder.class);
        log.info("RocketMQ 秒杀订单消费: {}", voucherOrder);
        // 保存订单
        voucherOrderService.save(voucherOrder);
        // 数据库秒杀库存减一
        Long voucherId = voucherOrder.getVoucherId();
        seckillVoucherService.update()
                .setSql("stock = stock - 1")
                .eq("voucher_id", voucherId)
                .gt("stock", 0)
                .update();
```

注意这里 DB 扣库存仍带 `.gt("stock", 0)` 条件更新，作为 Redis 之外的第二道防超卖。

---

## 四、延迟消息：超时关单（MQ 的第二重用法）

**投递**（同一个消费者里，`SeckillVoucherListener.java`）

```49:56:src/main/java/com/hmdp/listener/SeckillVoucherListener.java
        // 发送 RocketMQ 延迟消息：约 1 分钟后检查订单是否仍未支付
        // RocketMQ 默认 delayLevel: 1s 5s 10s 30s 1m 2m ... 这里用 5 表示 1 分钟
        rocketMQTemplate.syncSend(
                "order-timeout-topic",
                MessageBuilder.withPayload(String.valueOf(voucherOrder.getId())).build(),
                3000,
                5
        );
```

`syncSend(topic, msg, timeout, delayLevel)` 的第 4 个参数 `5` 即 1 分钟延迟等级。

**消费** `src/main/java/com/hmdp/listener/OrderTimeoutListener.java`

```24:28:src/main/java/com/hmdp/listener/OrderTimeoutListener.java
    public void onMessage(String orderIdStr) {
        Long orderId = Long.valueOf(orderIdStr);
        boolean closed = voucherOrderService.closeTimeoutOrder(orderId);
        log.info("超时关单结果 orderId={}, closed={}", orderId, closed);
    }
```

关单逻辑用状态机乐观锁保证幂等（见上一轮 `VoucherOrderServiceImpl:334-352` 的 `eq("status", 1)`）。

---

## 五、入口限流（配合削峰）

`src/main/java/com/hmdp/controller/VoucherOrderController.java`

```27:31:src/main/java/com/hmdp/controller/VoucherOrderController.java
    @RateLimiter(key = "seckill", windowSeconds = 1, count = 5, dimension = "user")
    @PostMapping("seckill/{id}")
    public Result seckillVoucher(@PathVariable("id") Long voucherId) {
        return voucherOrderService.seckillVoucher(voucherId);
    }
```

注解定义 `annotation/RateLimiter.java`，AOP 实现 `aspect/RateLimitAspect.java`（Redis + `sliding_window.lua`）。这里单用户 1 秒 5 次，把无效流量挡在 MQ 之前。

---

## 六、完整链路

```
HTTP 请求
  ↓ @RateLimiter 限流（Redis 滑动窗口）
  ↓ Lua 原子校验库存 + 一人一单
  ↓ convertAndSend("seckill-order-topic")   ← 削峰点，立即返回 orderId
  ───────────── 请求线程结束 ─────────────
  ↓ SeckillVoucherListener 消费
     ├─ save(order)            落单
     ├─ stock-1 where stock>0  扣库存
     └─ syncSend(delayLevel=5) 发延迟消息
  ↓ OrderTimeoutListener 消费（1 分钟后）
     └─ closeTimeoutOrder() 乐观锁关单 + 回补库存
```

---

## 七、与 README 描述的差距

README `194-197` 行声称「订单 ID 主键唯一 → 重复消费第二次 insert 失败即幂等」，但 `SeckillVoucherListener:40` 是裸 `save(voucherOrder)`，**没有捕获 `DuplicateKeyException`**，重复消费会直接抛异常触发 MQ 无限重试，且第 43 行的库存扣减不受主键约束保护。建议改成：

```java
try {
    voucherOrderService.save(voucherOrder);
} catch (DuplicateKeyException e) {
    log.warn("重复消费，orderId={}", voucherOrder.getId());
    return;  // 直接 ACK，不扣库存
}
```

需要我改吗？



# CityHub 基于Redis + AOP + 注解实现滑动窗口限流，支持全局/IP/用户多维度，防止系统过载、刷券、爬虫。代码在哪里

滑动窗口限流由 **4 个文件**构成，是标准的「注解 + 切面 + Lua」三段式。

## 一、注解定义（声明式入口）

`src/main/java/com/hmdp/annotation/RateLimiter.java`

```11:24:src/main/java/com/hmdp/annotation/RateLimiter.java
public @interface RateLimiter {

    /** 限流 key 前缀 */
    String key() default "rate:limit";

    /** 时间窗口（秒） */
    long windowSeconds() default 60;

    /** 窗口内最大请求数 */
    int count() default 10;

    /** 维度：global / ip / user */
    String dimension() default "global";
}
```

## 二、Lua 脚本（滑动窗口算法）

`src/main/resources/sliding_window.lua`

```11:21:src/main/resources/sliding_window.lua
-- 移除窗口外的时间戳
redis.call('zremrangebyscore', key, 0, now - window * 1000)

-- 当前窗口内请求数
local current = redis.call('zcard', key)
if current < limit then
    redis.call('zadd', key, now, now .. '-' .. math.random(100000, 999999))
    redis.call('expire', key, window)
    return 1
end
return 0
```

用 **ZSET** 实现：score = 请求时间戳（毫秒），member = 唯一随机串。三步 `zremrangebyscore → zcard → zadd` 在一个 Lua 内原子执行，避免计数竞态；相比固定窗口计数器，不会出现窗口临界点 2 倍流量突刺。

## 三、AOP 切面（多维度 key 拼装 + 拦截）

`src/main/java/com/hmdp/aspect/RateLimitAspect.java`

**脚本预加载**
```28:34:src/main/java/com/hmdp/aspect/RateLimitAspect.java
    private static final DefaultRedisScript<Long> SLIDING_WINDOW_SCRIPT;

    static {
        SLIDING_WINDOW_SCRIPT = new DefaultRedisScript<>();
        SLIDING_WINDOW_SCRIPT.setLocation(new ClassPathResource("sliding_window.lua"));
        SLIDING_WINDOW_SCRIPT.setResultType(Long.class);
    }
```

**环绕通知**
```39:57:src/main/java/com/hmdp/aspect/RateLimitAspect.java
    @Around("@annotation(com.hmdp.annotation.RateLimiter)")
    public Object around(ProceedingJoinPoint joinPoint) throws Throwable {
        MethodSignature signature = (MethodSignature) joinPoint.getSignature();
        Method method = signature.getMethod();
        RateLimiter rateLimiter = method.getAnnotation(RateLimiter.class);

        String key = buildKey(rateLimiter);
        Long allowed = stringRedisTemplate.execute(
                SLIDING_WINDOW_SCRIPT,
                java.util.Collections.singletonList(key),
                String.valueOf(rateLimiter.windowSeconds()),
                String.valueOf(rateLimiter.count()),
                String.valueOf(System.currentTimeMillis())
        );

        if (allowed == null || allowed == 0L) {
            return Result.fail("操作过于频繁，请稍后再试");
        }
        return joinPoint.proceed();
    }
```

**多维度 key 生成（核心）**
```60:71:src/main/java/com/hmdp/aspect/RateLimitAspect.java
    private String buildKey(RateLimiter rateLimiter) {
        String base = rateLimiter.key();
        String dimension = rateLimiter.dimension();
        if ("ip".equalsIgnoreCase(dimension)) {
            return base + ":ip:" + clientIp();
        }
        if ("user".equalsIgnoreCase(dimension)) {
            Long userId = UserHolder.getUser() == null ? 0L : UserHolder.getUser().getId();
            return base + ":user:" + userId;
        }
        return base;
    }
```

**真实 IP 提取**（`73-86` 行）：优先 `X-Forwarded-For`，取逗号分隔的第一段，回退 `getRemoteAddr()`。

## 四、落地使用（防刷券 / 防爬虫）

| 位置                             | 配置                                                    | 防护目标             |
| -------------------------------- | ------------------------------------------------------- | -------------------- |
| `VoucherOrderController.java:27` | `key="seckill", window=1s, count=5, dimension="user"`   | 单用户刷券/脚本抢购  |
| `ShopController.java:36`         | `key="shop:query", window=1s, count=20, dimension="ip"` | 爬虫高频抓取店铺详情 |

```27:28:src/main/java/com/hmdp/controller/VoucherOrderController.java
    @RateLimiter(key = "seckill", windowSeconds = 1, count = 5, dimension = "user")
    @PostMapping("seckill/{id}")
```

AOP 依赖：`pom.xml:70-73` 引入 `aspectjweaver`，Spring Boot 自动装配 `AopAutoConfiguration` 开启 `@EnableAspectJAutoProxy`。

## 五、执行链路

```
请求 → CGLIB 代理 → RateLimitAspect.around()
        ├─ 读注解 → buildKey() 拼出 seckill:user:1001
        ├─ Lua: zremrangebyscore(过期清理) → zcard(计数) → zadd(记录)
        ├─ 返回 0 → Result.fail("操作过于频繁") 短 circuit，不进业务
        └─ 返回 1 → joinPoint.proceed() 放行
```

---

## 六、可改进点

| #    | 问题                                                         | 位置                    |
| ---- | ------------------------------------------------------------ | ----------------------- |
| 1    | **无降级**：Redis 宕机时 `execute()` 抛异常 → 限流组件反成故障源，接口全 500。应 catch 后放行并告警 | `RateLimitAspect:46`    |
| 2    | **IP 可伪造**：无条件信任 `X-Forwarded-For`，爬虫随机改头即可绕过。生产应校验可信代理层级 | `RateLimitAspect:79-84` |
| 3    | **member 可能碰撞**：`now .. '-' .. math.random(100000,999999)` 同毫秒同随机数会被 ZSET 去重，少计一次。建议 member 由 Java 侧传 UUID | `sliding_window.lua:17` |
| 4    | **未登录用户共享桶**：`userId = 0` 让所有匿名用户共用一个限流桶，易误伤 | `RateLimitAspect:67`    |
| 5    | **时间取自应用服务器**：多节点时钟漂移会导致窗口判定偏差，高并发场景建议用 Redis `TIME` 命令 | `RateLimitAspect:51`    |
| 6    | **HTTP 语义缺失**：被限流仍返回 200，应返回 429 + `Retry-After` | `RateLimitAspect:55`    |
| 7    | **不支持多规则叠加**：注解未加 `@Repeatable`，无法同时配置「用户 5/s + IP 100/s」 | `RateLimiter.java:11`   |

需要我按这几点加固吗？



# 基于延迟队列实现分钟级定时任务，精准扫描并自动关闭超时未支付订单，有效释放被占用的库存资源。代码体现在哪里

需要先说明一点：**本项目没有用 Redis ZSET 或 JDK `DelayQueue`，而是用 RocketMQ 延迟消息实现的**。代码分布在 4 处。

## 一、投递延迟消息（延迟队列的「入队」）

`src/main/java/com/hmdp/listener/SeckillVoucherListener.java`

```49:56:src/main/java/com/hmdp/listener/SeckillVoucherListener.java
        // 发送 RocketMQ 延迟消息：约 1 分钟后检查订单是否仍未支付
        // RocketMQ 默认 delayLevel: 1s 5s 10s 30s 1m 2m ... 这里用 5 表示 1 分钟
        rocketMQTemplate.syncSend(
                "order-timeout-topic",
                MessageBuilder.withPayload(String.valueOf(voucherOrder.getId())).build(),
                3000,
                5
        );
```

`syncSend(topic, payload, timeout, delayLevel)` 的第 4 个参数 `5` 就是延迟等级 —— RocketMQ 18 个等级为 `1s 5s 10s 30s 1m 2m 3m 4m 5m 6m 7m 8m 9m 10m 20m 30m 1h 2h`，等级 5 = **1 分钟**。

注意这里只传了 `orderId`（`voucherOrder.getId()`），不传整个订单对象，避免消息体携带过期快照。

## 二、消费延迟消息（延迟队列的「出队」）

`src/main/java/com/hmdp/listener/OrderTimeoutListener.java`

```18:29:src/main/java/com/hmdp/listener/OrderTimeoutListener.java
@RocketMQMessageListener(topic = "order-timeout-topic", consumerGroup = "order-timeout-consumer")
public class OrderTimeoutListener implements RocketMQListener<String> {

    @Resource
    private VoucherOrderServiceImpl voucherOrderService;

    @Override
    public void onMessage(String orderIdStr) {
        Long orderId = Long.valueOf(orderIdStr);
        boolean closed = voucherOrderService.closeTimeoutOrder(orderId);
        log.info("超时关单结果 orderId={}, closed={}", orderId, closed);
    }
}
```

这里注入的是被 Spring 代理后的 `VoucherOrderServiceImpl`，所以下游 `@Transactional` 生效。

## 三、关单 + 释放库存（幂等核心）

`src/main/java/com/hmdp/service/impl/VoucherOrderServiceImpl.java`

```334:352:src/main/java/com/hmdp/service/impl/VoucherOrderServiceImpl.java
    @Transactional
    public boolean closeTimeoutOrder(Long orderId) {
        VoucherOrder order = getById(orderId);
        if (order == null || order.getStatus() == null || order.getStatus() != 1) {
            return false;
        }
        boolean updated = update()
                .eq("id", orderId)
                .eq("status", 1)
                .set("status", 4)
                .update();
        if (updated) {
            seckillVoucherService.update()
                    .setSql("stock = stock + 1")
                    .eq("voucher_id", order.getVoucherId())
                    .update();
        }
        return updated;
    }
```

**幂等关键在 `.eq("status", 1)`**：这条 UPDATE 天然是 CAS，影响行数为 0 就说明订单已被支付/取消，直接跳过，绝不重复释放库存。这正好回答了 Markdown 文档里担心的「重复关单重复加库存」。

对应的竞态另一侧是支付回调 `payCallback`（`319-328` 行），同样是 `eq("id").eq("status", 1).set("status", 2)` 的乐观锁。关单和支付回调抢同一个 `status=1`，谁先成功谁生效。

## 四、状态定义与初始落库

`src/main/java/com/hmdp/entity/VoucherOrder.java`

```51:53:src/main/java/com/hmdp/entity/VoucherOrder.java
    /**
     * 订单状态，1：未支付；2：已支付；3：已核销；4：已取消；5：退款中；6：已退款
     */
    private Integer status;
```

`SeckillVoucherListener` 落单时 `new VoucherOrder()` 并未 set status（null），但建表语句有兜底：

```1271:1271:src/main/resources/db/hmdp.sql
  `status` tinyint(1) UNSIGNED NOT NULL DEFAULT 1 COMMENT '订单状态，1：未支付；2：已支付；3：已核销；4：已取消；5：退款中；6：已退款',
```

MyBatis-Plus 默认 `insertStrategy = NOT_NULL` 会剔除 null 字段，落库时走 DB 默认值 `1`，所以订单一诞生就是「未支付」，延迟消息到点时能命中。

---

## 五、完整链路

```
seckillVoucher 返回 orderId
   ↓ Lua 校验通过 → convertAndSend("seckill-order-topic")
SeckillVoucherListener 消费
   ├─ save(order)              status 默认 1（未支付）
   ├─ stock-1 where stock>0
   └─ syncSend("order-timeout-topic", delayLevel=5)   ← 入延迟队列
        ↓ 1 分钟后投递
OrderTimeoutListener 消费
   └─ closeTimeoutOrder()
        ├─ UPDATE status 1→4 WHERE status=1   ← 乐观锁，失败即已支付
        └─ 成功则 stock+1                      ← 释放库存
```

选型理由见 `README.md:216-224`，对比了定时扫库 / Redis 过期监听（惰性删除不保证实时）后选了延迟消息。

---

## 六、发现的问题

| #    | 问题                                                         | 位置                              |
| ---- | ------------------------------------------------------------ | --------------------------------- |
| 1    | **只回补 MySQL，没回补 Redis**：`stock+1` 后未 `increment(SECKILL_STOCK_KEY + voucherId)`，也未 `srem` 清理 `seckill:order:{voucherId}` 中的 userId → 关单后用户无法再抢，且 Redis/MySQL 库存永久漂移 | `VoucherOrderServiceImpl:345-349` |
| 2    | **延迟时间硬编码**：魔法数字 `5` + `3000`，改时长要动代码。建议提到 `@Value("${order.timeout.delay-level:5}")` | `SeckillVoucherListener:51-56`    |
| 3    | **回补库存无上限保护**：若运营手动调过库存，回补可能超过活动原始总量 | `VoucherOrderServiceImpl:346-349` |
| 4    | **`getById` 前置校验多余且有竞态**：真正的幂等靠 `.eq("status",1)`，前置查询在读已提交隔离级别下可被并发支付穿插，建议删除让 SQL 全权判定 | `VoucherOrderServiceImpl:336-339` |
| 5    | **延迟消息发送失败会重投整个下单消息**：`syncSend` 异常冒泡到 `onMessage`，RocketMQ 重投 → 订单重复 `save`（叠加上一轮说的幂等缺失） | `SeckillVoucherListener:51`       |
| 6    | **RocketMQ 5.x 已支持任意秒级定时消息**，若 NameServer 是 5.x，可用 `Message#setDelayTimeSec(60)` 摆脱 18 个固定等级的约束 | `SeckillVoucherListener:51-56`    |

其中第 1 点是功能性缺陷，需要我修复吗？